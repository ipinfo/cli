# 3.3.2

- Errors now go to stderr instead of stdout.
- Windows release zip now contains `range2cidr.exe` instead of the versioned filename.
- Install scripts (`deb.sh`, `windows.ps1`) now fail loudly when installation does not succeed.

# 1.3.0

- Fixed range2cidr not properly re-writing headers when they were available.
